kind: Deployment
apiVersion: apps/v1
metadata:
  name: nginx-app
  namespace: gitops
  annotations:
    environment: prod
    organization: sales
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest